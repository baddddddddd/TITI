<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join Course | Student Portal</title>

  <style>
    @import url("/styles/common.css");

    .content-wrapper {
      margin: 0% 10%;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .form-container {
      width: 100%;
      max-width: 400px;
      text-align: center;
      border-radius: 12px;
      padding: 2rem 3.5rem;
      display: flex;
      flex-direction: column;
      row-gap: 1rem;
      background: white;
      box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.2);
    }

    .heading {
      font-size: 1.5rem;
      color: var(--main-color);
      font-weight: 700;
    }

    .field-container {
      width: 100%;
    }

    .field-container > input {
      width: 100%;
      padding: 0.5rem;
      border-radius: 5px;
    }

    @media screen and (max-width: 600px) {
      .form-container {
        padding: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="content-wrapper">
    <form class="form-container" action="/course/join" method="post" id="courseForm">
      <div class="heading">
        Join Course
      </div>
      <div class="field-container">
        <input required type="text" name="courseCode" id="courseCode" placeholder="Course Code">
      </div>
      <div class="error-message"><%= locals.errorMessage %></div>
      <div>
        <input class="btn" type="submit" value="JOIN">
      </div>
    </form>
  </div>
</body>
</html>